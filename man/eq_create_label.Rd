% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eq_map.R
\name{eq_create_label}
\alias{eq_create_label}
\title{Generate HTML Popup Labels for Earthquake Maps in earthquake_R}
\usage{
eq_create_label(data)
}
\arguments{
  \item{data}{A data frame containing NOAA earthquake data, typically processed using \code{eq_clean_data()} and \code{eq_location_clean()}.}
}
\value{
A character vector of HTML-formatted labels intended to be used as popup annotations in \code{eq_map()}.
}
\description{
\code{eq_create_label} assembles informative HTML popup labels for earthquake maps. It combines key details of each earthquake event—such as location, magnitude, and total deaths—into a single formatted string.
}
\details{
This function creates a comprehensive label for each earthquake record by concatenating:
\itemize{
  \item \strong{Location:} the cleaned location name.
  \item \strong{Magnitude:} the earthquake's magnitude (from the \code{EQ_PRIMARY} column).
  \item \strong{Total Deaths:} the total number of deaths (from the \code{TOTAL_DEATHS} column), if available.
}
These labels are designed for use with the \code{eq_map()} function by assigning the output to the \code{annot_col} parameter.
}
\examples{
\dontrun{
# Load and process the NOAA earthquake data
NOAA_data <- NOAA_data %>%
  eq_clean_data() %>%
  eq_location_clean() %>%
  dplyr::filter(COUNTRY %in% c("HONDURAS", "MEXICO") & lubridate::year(DATE) >= 2000) %>%
  dplyr::mutate(popup_text = eq_create_label(.))

# Create an interactive earthquake map with custom popup labels
eq_map(data = NOAA_data, annot_col = "popup_text")
}
}
\keyword{map}
