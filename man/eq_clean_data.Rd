% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_data.R
\name{eq_clean_data}
\alias{eq_clean_data}
\title{Clean NOAA Earthquake Data for earthquake_R}
\usage{
eq_clean_data(raw_data)
}
\arguments{
  \item{raw_data}{A data frame containing raw NOAA earthquake data.}
}
\value{
A \code{tibble} with cleaned earthquake data. This tibble includes a new \code{DATE} column (of type \code{Date}), created by combining the \code{YEAR}, \code{MONTH}, and \code{DAY} columns, and ensures that the \code{LATITUDE}, \code{LONGITUDE}, \code{EQ_PRIMARY}, \code{TOTAL_DEATHS}, and \code{FOCAL_DEPTH} columns are numeric. Other columns from the original data frame are preserved.
}
\description{
\code{eq_clean_data} cleans raw NOAA earthquake data by performing necessary data transformations, including type conversion and the creation of a standardized date column.
}
\details{
This function processes NOAA earthquake data as follows:
\itemize{
  \item Converts the \code{LATITUDE}, \code{LONGITUDE}, \code{EQ_PRIMARY}, \code{TOTAL_DEATHS}, and \code{FOCAL_DEPTH} columns to numeric.
  \item Replaces missing values in the \code{MONTH} and \code{DAY} columns with 1.
  \item Creates a new \code{DATE} column using \code{YEAR}, \code{MONTH}, and \code{DAY} via \code{lubridate::make_date()}.
  \item Cleans the \code{LOCATION_NAME} column by calling \code{eq_location_clean()}.
  \item Reorders columns to place \code{DATE}, \code{LATITUDE}, \code{LONGITUDE}, \code{LOCATION_NAME}, \code{COUNTRY}, \code{EQ_PRIMARY}, \code{FOCAL_DEPTH}, and \code{DEATHS} at the beginning.
}
}
\examples{
# Load example NOAA earthquake data
data(NOAA_data)
# Clean the raw NOAA earthquake data
clean_data <- eq_clean_data(NOAA_data)
head(clean_data)
}
\keyword{data}
