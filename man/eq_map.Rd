% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eq_map.R
\name{eq_map}
\alias{eq_map}
\title{Interactive Leaflet Map for Earthquake Data in earthquake_R}
\usage{
eq_map(data, annot_col = "DATE", provider = "Esri.NatGeoWorldMap")
}
\arguments{
  \item{data}{A data frame containing NOAA earthquake data that has been processed using \code{eq_clean_data()} and \code{eq_location_clean()}.}
  \item{annot_col}{A character string (of length one) specifying the name of the column to be used for annotation in the popup windows.}
  \item{provider}{An optional character string specifying the tile provider for the base map. Defaults to \code{"Esri.NatGeoWorldMap"}.}
}
\value{
A \code{leaflet} map object.
}
\description{
\code{eq_map} creates an interactive leaflet map displaying the epicenters of earthquakes. Each earthquake is represented by a circle marker, where the radius of the marker is proportional to the earthquake's magnitude. Popup windows display annotation information extracted from a specified column.
}
\details{
This function uses the \code{LONGITUDE} and \code{LATITUDE} columns from the input data frame to position the markers. It converts the specified annotation column to a character vector and assigns it to the popup parameter. Additionally, users can change the base map tile provider by setting the \code{provider} parameter to another valid provider (e.g., \code{"OpenStreetMap"}).
}
\examples{
\dontrun{
# Load and process NOAA earthquake data
NOAA_data <- NOAA_data %>%
  eq_clean_data() %>%
  eq_location_clean() %>%
  dplyr::filter(COUNTRY == "MEXICO" & lubridate::year(DATE) >= 2000)

# Create an interactive map using the default tile provider
eq_map(data = NOAA_data, annot_col = "DATE")

# Create an interactive map using an alternative tile provider
eq_map(data = NOAA_data, annot_col = "EQ_PRIMARY", provider = "OpenStre
